import { CategoryType } from "./CategoryType";

export class GeneratedTest {
  name: string;
  content: string;
  time: number;  
  openQuestion: number;
  closedQuestion: number;
  requiredPercentage: number;
  testCategories: Array<CategoryType>;
}